<!DOCUMENT html>
<html>
  <head>
  </head>
  <body>
      <ul>
        <% @games.each do |game| %>
          <li><%= link_to game.name, game_path(game) %></li>
        <% end %>
      </ul>
  </body>
</html>
